apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "console.serviceAccount" . }}
  labels:
    {{- include "userclouds.labels" . | nindent 4 }}
  annotations:
    {{- with .Values.roles.console }}
    eks.amazonaws.com/role-arn: {{ toYaml . }}
    {{- end }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: console
  labels:
    {{- include "userclouds.labels" . | nindent 4 }}
  annotations:
    checksum/config: {{ include (print $.Template.BasePath "/configs.yaml") . | sha256sum }}
spec:
  replicas: {{ .Values.console.replicas | default .Values.console.replicas }}
  selector:
    matchLabels:
      {{- include "console.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        userclouds.sla: critical
        {{- include "console.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.console.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.console.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.console.serviceAccountName }}
      serviceAccountName: {{ toYaml . }}
      {{- end }}
      volumes:
        - name: configmap
          configMap:
            name: userclouds-configs
            items:
              - key: base
                path: {{ printf "base_%s.yaml" .Values.universe }}
              - key: console
                path: {{ printf "console/%s.yaml" .Values.universe }}
      {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: console
          image: "{{ include "userclouds.image" . }}"
          imagePullPolicy: IfNotPresent
          {{- with .Values.tools.workingDir }}
          workingDir: {{ toYaml . }}
          {{- end }}
          {{- with .Values.console.command }}
          command:
            {{ toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.console.args }}
          args:
            {{ toYaml . | nindent 12 }}
          {{- end }}
          {{- include "userclouds.envConfigMap" . | nindent 10 }}
          env:
            {{- include "userclouds.envVars" . | nindent 10 }}
            {{- with .Values.extraEnv }}
            {{ toYaml . | nindent 10 }}
            {{- end }}
          {{- with .Values.console.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- include "userclouds.lifecycleDrain" . | nindent 10 }}
          volumeMounts:
            - name: configmap
              mountPath: /userclouds/configmaps
          {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: console
              containerPort: 5000
              protocol: TCP
            - name: internal
              containerPort: 5001
              protocol: TCP
          {{- with .Values.console.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.console.livenessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: console
  labels:
    {{- include "userclouds.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
      {{- include "console.selectorLabels" . | nindent 6 }}
  ports:
    - name: service
      protocol: TCP
      port: 80
      targetPort: 5000
    - name: internal
      protocol: TCP
      port: 5001
