apiVersion: v1
kind: ConfigMap
metadata:
  name: userclouds-env-variables
  labels:
    {{- include "userclouds.labels" . | nindent 4 }}
data:
    UC_REGION: {{ .Values.region | quote }}
    UC_UNIVERSE: {{ .Values.universe | quote }}
    UC_SECRET_MANAGER: kubernetes
